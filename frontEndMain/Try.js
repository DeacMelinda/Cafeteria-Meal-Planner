import { useHistory } from "react-router-dom";

function Try() {
    const history = useHistory();
    
    return(
        <div>
            <p>Hello!</p>
        </div>
    );
}

export default Try;